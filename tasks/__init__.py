# ============================================
# 包名称: tasks
# 包用途: 存放所有独立任务模块，每个模块对应一个可勾选的功能
# 设计原则: 每个任务函数必须优先判断勾选状态，未勾选直接跳过
# ============================================

# 本文件标记tasks目录为Python包，允许外部通过 from tasks.xxx import xxx 调用
